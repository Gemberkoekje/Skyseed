# Modpack visuals — open items only

> **Shipped:** the shader/visual stack (Complementary Reimagined default, Fresh Animations, ambient sounds, sound
> physics) is in and pinned. See the changelogs + git. Trimmed to OPEN items.

## What's left
- **#21 Distant Horizons (LOD)** — OPTIONAL, unblocked: drop the NeoForge 1.21.1 DH jar into `overrides/mods/`, wire
  config, verify shader compat (Iris + Monocle + Complementary), re-run `gen-mods-txt.ps1`. Only if wanted.
- **#55 shaderPack pin** — STANDING RULE (pin currently correct): on any Complementary/Euphoria Patches update,
  refresh the pin in `overrides/config/iris.properties`.
- **#53 Vanilla Tweaks 16×** — DROPPED / revivable: keep the §2 recipe (site-generated zip into
  `overrides/resourcepacks/` + enable in `options.txt`).
- **#54 standalone Skyseed resource pack** — DROPPED / revivable: keep the §3 re-scaffold recipe.

## Keep as reference
- §1 shader reference table (Iris / Monocle / Euphoria / Complementary path) — the how-to for #21/#55.
