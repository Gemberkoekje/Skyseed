# Immersive Engineering + Petroleum + flight — open items only

> Child of CONTENTPLAN. **Built end-to-end** and both nodes green — the IE ore island (6 `immersiveengineering_*`
> overrides: aluminum/lead/nickel/silver/uranium + a deep crude-oil pocket, inert-safe), the Excavator config pin
> (`chance` 0.9→0.7), the #45 IE + flight quest chapter, and the IE×FD compat. See `CHANGELOG_1.21.1.md` /
> `CHANGELOG_26.1.md` + git for the built history. Trimmed to the OPEN items (all in-game sign-offs — none headless).

## What's left (in-game only)
- Excavator yields veins over the void under a grown island; fine-tune `chance` (0.7).
- Crude-oil source survives grow-in (no flow-away) **and** is IE-Fluid-Pump-extractable; tune `chance` (0.15).
  Fallback if not pumpable: author a recipe route.
- All five IE metals appear at low/mid/high throws.
- Diesel-fuelled aircraft consumes fuel; the IE + flight quest chapters load.
- **#39** FE-flow in-game sign-off (Create → IE/AE2 across islands) — shared with the AE2 line.
